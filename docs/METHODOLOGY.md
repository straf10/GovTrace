# Μεθοδολογία

**Έκδοση:** 0.5 · **Τελευταία ενημέρωση:** 2026-07-10 · **Άδεια:** CC BY 4.0

> **Κεντρική αρχή — διαβάστε πρώτα αυτό:**
> Οι δείκτες που δημοσιεύονται εδώ είναι **στατιστικοί δείκτες ρίσκου διαδικασιών**, όχι διαπιστώσεις παρανομίας, διαφθοράς ή κακοδιαχείρισης. Ένας υψηλός δείκτης σημαίνει «μοτίβο που, σύμφωνα με τη διεθνή βιβλιογραφία, δικαιολογεί περαιτέρω διερεύνηση» — τίποτα περισσότερο. Όπως το διατυπώνει το Red Flags Project (Ουγγαρία): *«risky does not mean corrupt»*. Πολλά flags έχουν απολύτως νόμιμες εξηγήσεις (επείγουσες ανάγκες, εξειδικευμένες αγορές με έναν προμηθευτή, μικρά δείγματα, νησιωτικότητα κ.ά.).

---

## 1. Σκοπός

Η πλατφόρμα συγκεντρώνει, κανονικοποιεί και οπτικοποιεί **δημόσια, ανοιχτά δεδομένα** δημοσίων συμβάσεων της Ελλάδας, και υπολογίζει πάνω σε αυτά **επικυρωμένους από τη βιβλιογραφία δείκτες ακεραιότητας διαδικασιών** (procurement integrity indicators). Στόχοι:

1. Να μπορεί κάθε πολίτης να δει τι αγοράζει ο δήμος, η περιφέρεια ή το υπουργείο του, από ποιον και με ποια διαδικασία.
2. Να αναδεικνύονται **μοτίβα** που η διεθνής έρευνα συσχετίζει με αυξημένο ρίσκο μειωμένου ανταγωνισμού — πάντα με πλήρη πρόσβαση στα πρωτογενή δεδομένα και στη μεθοδολογία υπολογισμού.
3. Να παρέχεται στους φορείς και τις εταιρείες **δικαίωμα απάντησης/διόρθωσης** (βλ. [DISCLAIMER.md](DISCLAIMER.md)).

Ό,τι δημοσιεύεται συνοδεύεται από: (α) τα raw δεδομένα από τα οποία προέκυψε, (β) link στο πρωτότυπο ΑΔΑΜ/ΑΔΑ, (γ) την ακριβή φόρμουλα υπολογισμού, (δ) τους περιορισμούς της.

---

## 2. Πηγές δεδομένων

### 2.1 ΚΗΜΔΗΣ Open Data API (κύρια πηγή)

- **URL:** `https://cerpp.eprocurement.gov.gr/khmdhs-opendata`
- **Τεκμηρίωση:** [Swagger UI](https://cerpp.eprocurement.gov.gr/khmdhs-opendata/swagger-ui/index.html)
- **Άδεια δεδομένων:** Creative Commons Attribution 4.0 (CC BY 4.0)
- **Όριο χρήσης:** 350 requests/λεπτό (η υλοποίησή μας περιορίζεται συντηρητικά στα 300)

Το Κεντρικό Ηλεκτρονικό Μητρώο Δημοσίων Συμβάσεων (ΚΗΜΔΗΣ) είναι το **επίσημο μητρώο** στο οποίο οι αναθέτουσες αρχές υποχρεούνται εκ του νόμου (ν. 4412/2016) να καταχωρίζουν τις πράξεις τους. Οντότητες που αντλούμε:

| Οντότητα | Endpoint | Πρόθεμα ΑΔΑΜ | Περιεχόμενο |
|---|---|---|---|
| Αιτήματα | `POST /request` | `REQ` | Πρωτογενή αιτήματα δαπάνης |
| Προσκλήσεις/Προκηρύξεις/Διακηρύξεις | `POST /notice` | `PROC` | Δημοσίευση πρόθεσης σύναψης σύμβασης |
| Αναθέσεις | `POST /auction` | `AWRD` | Αποφάσεις ανάθεσης (κατακύρωσης) |
| Συμβάσεις | `POST /contract` | `SYMV` | Υπογεγραμμένες συμβάσεις |
| Εντολές Πληρωμών | `POST /payment` | `PAY` | Εντολές πληρωμής |
| Συνδεδεμένες Πράξεις | `GET /adamChain/{ΑΔΑΜ}` | — | Η αλυσίδα REQ→PROC→AWRD→SYMV→PAY |

Κρίσιμα πεδία κάθε εγγραφής (επιβεβαιωμένα από το API):

- `referenceNumber` — ο **ΑΔΑΜ** (μοναδικό αναγνωριστικό, π.χ. `25AWRD016745076`), που επιτρέπει σε οποιονδήποτε να επαληθεύσει την πράξη στο ΚΗΜΔΗΣ.
- `organizationVatNumber`, `organization` — ΑΦΜ και όνομα αναθέτουσας αρχής.
- `classificationOfPublicLawOrganization`, `typeOfContractingAuthority` — τύπος φορέα (π.χ. ΟΤΑ).
- `procedureType` — τύπος διαδικασίας· **η τιμή «Απευθείας ανάθεση» ξεχωρίζει τις απευθείας αναθέσεις**.
- `totalCostWithVAT`, `totalCostWithoutVAT`, `budget` — ποσά.
- `nutsCode`, `nutsCity`, `nutsPostalCode` — γεωγραφικός προσδιορισμός.
- `objectDetailsList[].cpvs` — κωδικοί CPV (κατηγοριοποίηση αντικειμένου), ποσότητες, μονάδες.
- `contractingDataDetails.contractingMembersDataList[]` — ΑΦΜ και όνομα αναδόχου/-ων.
- `noticeRefNo`, `contractRefNo`, `paymentRefNo`, `approvedRequestsList` — σύνδεσμοι της αλυσίδας ΑΔΑΜ.
- `submissionDate`, `signedDate`, `lastUpdateDate` — ημερομηνίες.
- `cancelled`, `amendPreviousAuction`, `isModified` — κατάσταση/τροποποιήσεις.

### 2.2 Μελλοντικές/συμπληρωματικές πηγές (δεν χρησιμοποιούνται ακόμα)

- **ΔΙΑΥΓΕΙΑ** (diavgeia.gov.gr): εγκρίσεις δαπανών, οριστικοποιήσεις πληρωμών, προϋπολογισμοί/απολογισμοί ΟΤΑ.
- **ΕΛΣΤΑΤ**: Δείκτης Τιμών Καταναλωτή και δείκτες κόστους κατασκευής, για αποπληθωρισμό ποσών.
- **ΓΕΜΗ Open Data** (Φάση 2): προφίλ εταιρειών-αναδόχων.
- **TED** (Φάση 3): διασταύρωση συμβάσεων άνω των ορίων ΕΕ.

Κάθε νέα πηγή θα τεκμηριώνεται εδώ **πριν** χρησιμοποιηθεί σε δημοσιευμένο δείκτη.

---

## 3. Συλλογή & επεξεργασία δεδομένων

1. **Άντληση**: μέσω του open-source client μας ([src/kimdis/client.py](src/kimdis/client.py)): σελιδοποίηση 50 εγγραφών/σελίδα, χρονικά παράθυρα ≤180 ημερών, όριο <350 αιτήματα/λεπτό, αυτόματη επανάληψη σε προσωρινά σφάλματα. Τα δεδομένα αποθηκεύονται **ως έχουν** (raw) σε μορφή Parquet.
2. **Έλεγχοι πληρότητας**: για κάθε batch μετράμε % εγγραφών με έγκυρο ΑΔΑΜ, ημερομηνία, ποσό, CPV, και ΑΦΜ σε δύο επίπεδα: **μορφή** (9 ψηφία μετά την κανονικοποίηση, `pct_org_vat`) και **checksum mod-11** (`pct_org_vat_checksum`, `is_valid_vat_checksum()` στο [kimdis_data.py](../scripts/kimdis_data.py)). Το keying/entity-resolution χρησιμοποιεί μόνο τον έλεγχο μορφής (κάποια ΑΦΜ δημοσίων φορέων στο ΚΗΜΔΗΣ δεν ακολουθούν πάντα το τυπικό checksum)· το checksum δημοσιεύεται ως ξεχωριστό ποιοτικό μέτρο, όχι ως φίλτρο αποδοχής. Τα reports πληρότητας δημοσιεύονται.
3. **Κανονικοποίηση**: ενιαίος πίνακας φορέων (ΑΦΜ → όνομα → τύπος → NUTS) και αναδόχων. Στη Φάση 2 προστίθεται entity resolution (deduplication παραλλαγών ονομάτων) με μετρημένη ακρίβεια σε χειροκίνητο δείγμα.
4. **Αποπληθωρισμός**: όπου συγκρίνονται ποσά μεταξύ ετών, μετατρέπονται σε σταθερές τιμές έτους βάσης με τον ΔΤΚ της ΕΛΣΤΑΤ. Ονομαστικά και αποπληθωρισμένα ποσά εμφανίζονται και τα δύο.
5. **Καμία χειροκίνητη παρέμβαση σε τιμές**: δεν διορθώνουμε, δεν συμπληρώνουμε και δεν «καθαρίζουμε» ποσά ή ονόματα πέρα από τεκμηριωμένους, αναπαραγώγιμους κανόνες που ζουν στον κώδικα αυτού του repo.

---

## 4. Δείκτες (v1)

Όλοι οι δείκτες v1 υπολογίζονται **αποκλειστικά από πεδία ΚΗΜΔΗΣ** και στηρίζονται σε επικυρωμένη βιβλιογραφία (βλ. §8). Κάθε δείκτης δημοσιεύεται μαζί με: το N (μέγεθος δείγματος), την περίοδο αναφοράς, και τα επιμέρους στοιχεία από τα οποία υπολογίστηκε.

### 4.1 Ποσοστό απευθείας αναθέσεων

- **Ορισμός:** Μερίδιο των αναθέσεων ενός φορέα που έγιναν με τη διαδικασία «Απευθείας ανάθεση», υπολογισμένο και σε **πλήθος** και σε **αξία**.
- **Φόρμουλα:** `DA_count = N_direct / N_total`, `DA_value = Σ(value_direct) / Σ(value_total)` ανά φορέα ανά έτος (προαιρετικά ανά CPV division).
- **Πεδία:** `procedureType`, `totalCostWithoutVAT`, `organizationVatNumber`.
- **Λογική:** Η απευθείας ανάθεση είναι νόμιμη κάτω από όρια του ν. 4412/2016, αλλά συστηματικά υψηλά ποσοστά σημαίνουν περιορισμένο ανταγωνισμό (OECD, EU Single Market Scoreboard).
- **Παγίδες:** μικροί φορείς με λίγες, μικρές προμήθειες θα έχουν φυσιολογικά υψηλό ποσοστό σε πλήθος· γι' αυτό δείχνουμε πάντα και το ποσοστό σε αξία και το N.

### 4.2 Συγκέντρωση αναδόχων (HHI & top-1 μερίδιο)

- **Ορισμός:** Πόσο συγκεντρωμένες είναι οι δαπάνες ενός φορέα σε λίγους αναδόχους.
- **Φόρμουλα:** `HHI = Σᵢ(sᵢ²)` όπου `sᵢ` το μερίδιο (0–1) του αναδόχου i στη συνολική αξία συμβάσεων του φορέα στην περίοδο· επίσης `top1 = max(sᵢ)`. HHI > 0,25 θεωρείται υψηλή συγκέντρωση (κατά αναλογία με τα όρια ανταγωνισμού).
- **Πεδία:** `contractingMembersDataList[].vatNumber`, `totalCostWithoutVAT`.
- **Παραδοχή κοινοπραξιών (v1):** το ΚΗΜΔΗΣ δεν δημοσιεύει επιμερισμό αξίας ανά μέλος κοινοπραξίας· όταν μια σύμβαση έχει περισσότερα από ένα `contractingMembersDataList[]`, **όλη η αξία πιστώνεται στον πρώτο καταχωρημένο ανάδοχο** (`members[0]`), όχι επιμερισμένη. Αυτό ευνοεί συστηματικά τον πρώτο εταίρο κοινοπραξιών στον υπολογισμό του HHI/top1. Για να φαίνεται το μέγεθος της πιθανής μεροληψίας, ο δείκτης δημοσιεύεται πάντα μαζί με το `pct_multi_member` (ποσοστό συμβάσεων του φορέα/έτους με >1 μέλος).
- **Παγίδες:** εξειδικευμένες αγορές (π.χ. συντήρηση συγκεκριμένου λογισμικού) έχουν εγγενώς λίγους προμηθευτές· ο δείκτης υπολογίζεται και ανά CPV division για να μετριάζεται αυτό. Απαιτείται ελάχιστο N συμβάσεων (≥10) για να δημοσιευθεί.

### 4.3 Single-bid rate

- **Ορισμός:** Μερίδιο ανταγωνιστικών διαδικασιών με μία μόνο προσφορά.
- **Φόρμουλα:** `SB = N_single_bid / N_competitive`, μόνο όπου τα δεδομένα προσφορών είναι διαθέσιμα.
- **Λογική:** Ο πιο ευρέως επικυρωμένος μεμονωμένος δείκτης στη βιβλιογραφία (Fazekas & Kocsis· EU Single Market Scoreboard — Ελλάδα: από τις υψηλότερες τιμές στην ΕΕ).
- **Παγίδες:** το πεδίο δεν υπάρχει σε όλες τις εγγραφές ΚΗΜΔΗΣ· ο δείκτης υπολογίζεται μόνο στο υποσύνολο με διαθέσιμα δεδομένα και το ποσοστό κάλυψης δημοσιεύεται δίπλα του.

### 4.4 Έλεγχος Benford (1ο & 2ο ψηφίο)

- **Ορισμός:** Στατιστική απόκλιση της κατανομής πρώτων/δεύτερων ψηφίων των ποσών ενός φορέα από την αναμενόμενη κατανομή Benford.
- **Φόρμουλα:** Κατανομή αναφοράς `P(d) = log₁₀(1 + 1/d)`. Μέτρο απόκλισης: **MAD** (Mean Absolute Deviation) με τα κατώφλια συμμόρφωσης του Nigrini· το χ² αναφέρεται συμπληρωματικά.
- **Πεδία:** `totalCostWithoutVAT` σε πληρωμές/συμβάσεις.
- **Παγίδες:** ο Benford **δεν είναι απόδειξη** κανενός — αποκλίσεις προκύπτουν φυσιολογικά από θεσμικά κατώφλια (π.χ. πολλές συμβάσεις ακριβώς κάτω από όριο), στρογγυλοποιημένους προϋπολογισμούς, ομοιογενείς αγορές. Απαιτεί μεγάλο N (≥300 εγγραφές) και χρησιμοποιείται μόνο ως δευτερεύον σήμα, ποτέ μόνος του.

### 4.5 Κατακερματισμός συμβάσεων (bid-splitting)

- **Ορισμός:** Ασυνήθιστη συγκέντρωση αναθέσεων ακριβώς **κάτω** από το εκάστοτε νόμιμο όριο απευθείας ανάθεσης.
- **Μέθοδος:** Ιστόγραμμα αξιών γύρω από το κατώφλι (π.χ. ±20%)· δείκτης = λόγος πυκνότητας στο διάστημα αμέσως κάτω από το όριο προς το διάστημα αμέσως πάνω (McCrary-style density test). Συμπληρωματικά: πλήθος «δίδυμων» αναθέσεων ίδιου φορέα, ίδιου CPV, σε κοντινές ημερομηνίες, με άθροισμα πάνω από το όριο.
- **Πεδία:** `totalCostWithoutVAT`, `procedureType`, `organizationVatNumber`, CPV, ημερομηνίες.
- **Παγίδες:** τα όρια έχουν αλλάξει διαχρονικά (καταγράφουμε τον πίνακα ορίων ανά περίοδο στον κώδικα)· η συγκέντρωση κάτω από το όριο είναι εν μέρει αναμενόμενη ορθολογική συμπεριφορά — σημασία έχει η **σύγκριση μεταξύ φορέων**, όχι η απόλυτη τιμή.

### 4.6 Ποσοστό έκπτωσης προκήρυξης → σύμβασης

- **Ορισμός:** Διαφορά εκτιμώμενης αξίας (προκήρυξη) και τελικής αξίας (σύμβαση).
- **Φόρμουλα:** `discount = (est_value − contract_value) / est_value`, μέσω σύνδεσης ΑΔΑΜ (`noticeRefNo` → `contractRefNo`).
- **Λογική:** Σε ανταγωνιστικές αγορές αναμένονται θετικές εκπτώσεις· **συστηματικά μηδενικές** εκπτώσεις σε ανταγωνιστικές διαδικασίες είναι καθιερωμένο red flag (OECD bid-rigging screens).
- **Παγίδες:** μεμονωμένη μηδενική έκπτωση δεν σημαίνει τίποτα· ο δείκτης είναι το **ποσοστό των διαδικασιών φορέα με ~0% έκπτωση**, όχι η έκπτωση μιας σύμβασης.

### 4.7 Σύντομες προθεσμίες υποβολής

- **Ορισμός:** Ημέρες από τη δημοσίευση της προκήρυξης έως την καταληκτική ημερομηνία υποβολής προσφορών.
- **Φόρμουλα:** `days = finalSubmissionDate − submissionDate` (πεδία notice, κανονικοποιημένα στο ημερολογιακό τους date πριν την αφαίρεση -- το `submissionDate` έχει ώρα/λεπτό ενώ το `finalSubmissionDate` είναι σχεδόν πάντα μεσάνυχτα). Δείκτης φορέα/έτους = **διάμεση προθεσμία** (ημέρες), ελάχιστο N=5 έγκυρες διαδικασίες (ίδιο κατώφλι με §4.5/§4.6 -- το §4.7 δεν όριζε ρητά ελάχιστο N, αποφασίστηκε ρητά session 18).
- **Αποκλεισμός απευθείας αναθέσεων:** το `typeOfProcedure.key == "6"` (Απευθείας ανάθεση) εξαιρείται -- είναι ~84% των notice εγγραφών, και εκεί το ζεύγος `submissionDate`/`finalSubmissionDate` δεν αντιστοιχεί σε πραγματική ανταγωνιστική περίοδο υποβολής (σχεδόν ταυτόχρονες τιμές, θα αλλοίωνε τη διάμεσο προς τα κάτω). Ο δείκτης υπολογίζεται μόνο επί ανταγωνιστικών διαδικασιών (Ανοιχτή/Κλειστή διαδικασία, διαπραγμάτευση με/χωρίς προκήρυξη, ανταγωνιστικός διάλογος).
- **Guard σε άκυρες τιμές:** εγγραφές με κενή ημερομηνία (NaT) ή αρνητική διάρκεια (`finalSubmissionDate` πριν το `submissionDate` -- data error, ~2-3% των εγγραφών σε δείγματα ελέγχου) εξαιρούνται από τη διάμεσο. Η κάλυψη (`coverage_pct` = έγκυρες / σύνολο ανταγωνιστικών διαδικασιών του φορέα/έτους) δημοσιεύεται πάντα δίπλα στον δείκτη.
- **Λογική:** Πολύ σύντομες προθεσμίες ευνοούν προμηθευτές με εκ των προτέρων γνώση (Fazekas CRI component, DIGIWHIST).
- **⚠ Ανοιχτό (εκκρεμεί, session 18):** το «% διαδικασιών με σύντομη προθεσμία» **δεν δημοσιεύεται ακόμα**. Απαιτεί πίνακα ελάχιστων νόμιμων προθεσμιών ανά τύπο διαδικασίας (EU Directive 2014/24 άρθρα 27-28 / ν. 4412/2016 άρθρα 121-122, με μειώσεις για ηλεκτρονική υποβολή/επείγον) που δεν είναι σήμερα τεκμηριωμένος εδώ ούτε επιβεβαιωμένος νομικά -- ίδιο ανοιχτό ζήτημα με τα όρια bid-splitting του §4.5 (βλ. changelog v0.2). Μέχρι τη νομική επιβεβαίωση, δημοσιεύεται μόνο η αδιαμφισβήτητη διάμεση προθεσμία.

### 4.8 Σύνθετος δείκτης (τύπου CRI)

- **Φόρμουλα:** Απλός (μη σταθμισμένος) μέσος όρος των διαθέσιμων επιμέρους flags του φορέα, κανονικοποιημένων στο [0,1]. Αν ένας δείκτης δεν υπολογίζεται (ανεπαρκές N), **εξαιρείται** από τον μέσο και αυτό σημειώνεται.
- **Κανόνας παρουσίασης:** Ο σύνθετος δείκτης **δεν εμφανίζεται ποτέ μόνος** — πάντα μαζί με όλα τα επιμέρους flags και τα N τους. Ακολουθούμε την πρακτική του Corruption Risk Index (Fazekas et al.) και του Government Transparency Institute.
- **Ερμηνεία:** Ο δείκτης μετρά **χαρακτηριστικά διαδικασιών**, όχι πρόσωπα ή προθέσεις. Κατατάξεις παρουσιάζονται με ουδέτερη γλώσσα («δείκτες διαφάνειας διαδικασιών»).

### 4.9 Κάλυψη δεδομένων (γνωστά κενά)

- **2 μόνιμα κενά μηνών σε `auction`:** `2021-02` και `2025-08` λείπουν οριστικά από το backfill. Αιτία: το ΚΗΜΔΗΣ API σταματά να απαντά με συνέπεια γύρω στη σελίδα ~15-16 (server-side timeout), ανεξάρτητο από τον ρυθμό αιτημάτων μας — επιβεβαιωμένο σε πολλαπλά backfill runs. Καταγράφονται και ξαναδοκιμάζονται αυτόματα σε κάθε μελλοντικό backfill run (`data/raw/_backfill_failures.json`).
- **Επίπτωση:** οι δείκτες των ετών **2021** και **2025** (Ποσοστό απευθείας αναθέσεων §4.1, HHI §4.2, συνολικές αξίες) υποεκτιμούν συστηματικά ~1/12 του έτους για την οντότητα `auction`. Οι δείκτες `contract`/`notice` δεν επηρεάζονται (πλήρη για τα ίδια έτη).
- **Discount rate (§4.6) — κάλυψη 2020:** μόνο ~6% των εγγραφών του 2020 έχουν σύνδεση notice↔auction (η πλειοψηφία εμφανίζεται ως «Ανεπαρκή δεδομένα»). Συνειδητή επιλογή, όχι σφάλμα: το `noticeRefNo` είναι σπάνια συμπληρωμένο στα πρώτα έτη του dataset.
- **Payment entity:** μόνο 2020-01..05 έχει κατέβει μέχρι στιγμής (§4.4 Benford εκκρεμεί μέχρι να ολοκληρωθεί το backfill του payment entity).

---

## 5. Κατώφλια δημοσίευσης & στατιστική προστασία

- Δείκτης φορέα δημοσιεύεται μόνο αν βασίζεται σε **επαρκές δείγμα** (ελάχιστα N ανά δείκτη, όπως ορίζονται στην §4). Κάτω από το κατώφλι εμφανίζεται «ανεπαρκή δεδομένα».
- Οι κατατάξεις συνοδεύονται από **διαστήματα/αβεβαιότητα** όπου υπολογίζονται (π.χ. Wilson intervals σε ποσοστά).
- Ακραίες τιμές ελέγχονται πρώτα για σφάλματα καταχώρισης (π.χ. ποσά ×1000) και επισημαίνονται ως πιθανά data errors, όχι ως ευρήματα.

## 6. Τι ΔΕΝ ισχυριζόμαστε

1. **Δεν** ισχυριζόμαστε ότι οποιοσδήποτε φορέας, εταιρεία ή πρόσωπο έχει διαπράξει παρανομία. Αυτό μπορεί να το κρίνει μόνο δικαστήριο ή αρμόδια ελεγκτική αρχή.
2. **Δεν** υπολογίζουμε «πραγματικό κόστος αγοράς» ή «υπερτιμολόγηση» συγκεκριμένης σύμβασης — δεν υπάρχουν τα δεδομένα για κάτι τέτοιο. Ό,τι συγκρίσεις τιμών γίνουν (Φάση 3) θα είναι **σχετικές συγκρίσεις εντός του ίδιου dataset** (π.χ. απόσταση από τη διάμεση τιμή μονάδας ίδιου CPV/έτους), με δημοσιευμένη φόρμουλα.
3. **Δεν** δημοσιεύουμε δεδομένα φυσικών προσώπων στη v1. Μόνο φορείς και νομικά πρόσωπα.
4. Συγκεντρωτικές εκτιμήσεις «δυνητικής εξοικονόμησης» (Φάση 3) θα παρουσιάζονται **μόνο ως εύρος σε εθνικό/κλαδικό επίπεδο** με ρητές παραδοχές, ποτέ ως ποσό που αποδίδεται σε συγκεκριμένο φορέα.

## 7. Διαφάνεια, αναπαραγωγιμότητα & changelog

- Όλος ο κώδικας συλλογής και υπολογισμού είναι open source σε αυτό το repository. Οποιοσδήποτε μπορεί να αναπαραγάγει κάθε δημοσιευμένο αριθμό.
- Κάθε αλλαγή σε φόρμουλα, κατώφλι ή πηγή δεδομένων καταγράφεται σε **changelog αλγορίθμων** (σε αυτό το αρχείο, §9) με ημερομηνία και αιτιολόγηση, και αποτυπώνεται στο git history.
- Λάθη που εντοπίζονται (από εμάς ή από τρίτους μέσω της διαδικασίας διόρθωσης) διορθώνονται και η διόρθωση δημοσιεύεται.

## 8. Βιβλιογραφία & πρότυπα

- Fazekas, M., Tóth, I. J., & King, L. P. — *An Objective Corruption Risk Index Using Public Procurement Data* (Corruption Risk Index / CRI).
- Fazekas, M. & Kocsis, G. — *Uncovering High-Level Corruption: Cross-National Objective Corruption Risk Indicators Using Public Procurement Data*.
- OECD — *Guidelines for Fighting Bid Rigging in Public Procurement* & *Analytics for Integrity*.
- DIGIWHIST / Government Transparency Institute — opentender.eu indicators methodology.
- European Commission — *Single Market Scoreboard, Public Procurement indicators*.
- Nigrini, M. — *Benford's Law: Applications for Forensic Accounting, Auditing, and Fraud Detection*.
- Red Flags Project (Ουγγαρία) — redflags.eu («risky does not mean corrupt»).
- Transparency International EU — *Integrity Watch*.
- ν. 4412/2016 (δημόσιες συμβάσεις), ν. 5090/2024 (τροποποιήσεις ΠΚ), άρθρα 362-363, 367 ΠΚ.

## 9. Changelog μεθοδολογίας

| Ημερομηνία | Έκδοση | Αλλαγή |
|---|---|---|
| 2026-07-08 | 0.1 | Αρχική έκδοση: ορισμοί δεικτών v1, πηγές, αρχές δημοσίευσης. Κανένας δείκτης δεν έχει δημοσιευθεί ακόμα. |
| 2026-07-08 | 0.2 | Πρώτη υλοποίηση §4.5 (bid-splitting) και §4.6 (discount rate) σε `scripts/compute_indicators_v1.py`, επί δεδομένων 2020-2021 + 2025-05. **§4.5:** πίνακας ορίων απευθείας ανάθεσης ανά περίοδο -- πριν 1/6/2021: 20.000€ (όλες οι κατηγορίες)· από 1/6/2021 (ν. 4782/2021): 30.000€ προμήθειες/υπηρεσίες, 60.000€ έργα/μελέτες/τεχνικές υπηρεσίες (κατηγοριοποίηση βάσει `contractType.value`). **Χρειάζεται νομική επιβεβαίωση πριν οριστική δημοσίευση** (πρώτο πέρασμα βάσει δημόσιας δευτερογενούς πηγής, όχι πρωτογενούς νομικού κειμένου). **§4.6:** η σύνδεση notice↔contract μέσω `contract.noticeReferenceNumber` είναι σχεδόν κενή στα δεδομένα (<0.1%, ελέγχθηκε επί 2020-01)· χρησιμοποιείται αντ' αυτής η σύνδεση `notice.referenceNumber` ↔ `auction.noticeRefNo` (κάλυψη: μόνο ~10-18% των auctions έχουν καν `noticeRefNo` -- αναμενόμενο, αφού οι απευθείας αναθέσεις δεν προηγούνται notice) και η τιμή κατακύρωσης (auction) ως proxy της τελικής τιμής σύμβασης αντί της υπογεγραμμένης σύμβασης. Το ποσοστό κάλυψης δημοσιεύεται πάντα δίπλα στον δείκτη. Προστέθηκε επίσης πίνακας κανονικοποίησης φορέων (`scripts/build_entity_table.py` → `data/processed/entities.csv`, ΑΦΜ→όνομα/τύπος/NUTS) και static dashboard v1 σκελετός (`site/`, χωρίς build step, τροφοδοτείται από `scripts/build_site_data.py`). |
| 2026-07-09 | 0.3 | Pre-release: εφαρμόστηκε το πλάνο του τεχνικού ελέγχου (`docs/tech_report.md`). **Ενιαίος persisted VAT resolver** (A3): `build_entity_table.py` γράφει πλέον `data/processed/vat_resolver.csv` (name→ΑΦΜ, χτισμένο από auction+contract+notice)· `compute_indicators_v1.py` και `build_foreas_data.py` τον διαβάζουν αντί να χτίζουν ο καθένας δικό του (πριν, το `build_foreas_data.py` χτίζε resolver μόνο από auctions, ~25% ΑΦΜ fill, με αποτέλεσμα ασύμφωνα κλειδιά ΑΦΜ ανάμεσα σε δείκτες και σελίδες προφίλ φορέα). **Έλεγχος πληρότητας pagination** (A1): το backfill πλέον επαληθεύει `totalElements` έναντι των πραγματικά κατεβασμένων εγγραφών· ελλιπείς μήνες δεν γράφονται καθόλου (αντί να γράφονται ως "πλήρεις" με λιγότερες γραμμές) και καταγράφονται σε `data/raw/_backfill_failures.json` για επανάληψη. Η **αναδρομική επαλήθευση/διόρθωση του ήδη κατεβασμένου 2020-2023 dataset** (audit A1 + γνωστά κενά `auction_2020_08`, `auction_2021_02`) εκκρεμεί ακόμα -- θα εκτελεστεί σε επόμενο run του `backfill_historical.py` και οι αριθμοί των δεικτών θα ενημερωθούν τότε αν αλλάξουν. |
| 2026-07-10 | 0.4 | **Sanity-check έναντι δημοσιευμένων εθνικών ποσοστών** (EU Single Market Scoreboard ~58,2% single-bidding, Vouliwatch 62,4% απευθείας σε συμβουλευτικές): το εθνικό `da_count_pct` (75-84% ανά έτος 2020-2026) αποκλίνει αλλά αναμενόμενα -- διαφορετικός πληθυσμός (όλες οι αναθέσεις έναντι μόνο ανταγωνιστικών/άνω ενωσιακού ορίου διαδικασιών). Το `da_value_pct` όμως ταλαντευόταν χωρίς λογική έτος προς έτος (33%→97%→41%→98%→46%→70%→6%), λόγω **20 εγγραφών `totalCostWithVAT`/`totalCostWithoutVAT` >1 δισ. ευρώ** (έως €600 δισ.) με fingerprint φθαρμένης πηγής (π.χ. ΑΦΜ "11111111111" με ποσό ακριβώς 111.111.111.111€· Δήμος Αγίας Παρασκευής €600.030.600.000 σε μία ανάθεση με μέσο όρο υπόλοιπων 802 αναθέσεών του €13.600). **Προστέθηκε `VALUE_SANITY_CAP` = 10 δισ. ευρώ** σε `scripts/kimdis_data.py` (`sanitize_value()`), εφαρμοσμένο σε όλα τα σημεία παραγωγής `value` από τα πεδία κόστους σε `compute_indicators_v1.py` και `build_foreas_data.py` (και στο per-εγγραφή `amount_with_vat` της προβολής "πρόσφατη δραστηριότητα"). Τιμές πάνω από το όριο μηδενίζονται (όχι clip) ώστε να μη διαστρεβλώνουν sums/rankings/terciles μεγέθους. Το όριο χωρίζει καθαρά τις 9 εγγραφές-σκουπίδια από τις μεγαλύτερες αλλά εύλογες τιμές που μένουν (π.χ. ΑΔΜΗΕ €2,06δισ., Υπ. Εθνικής Άμυνας €4,26δισ.). |
| 2026-07-10 | 0.4 | **Ολοκλήρωση Sprint C backfill re-run** (η εκκρεμότητα της v0.3): 238 parquet αρχεία, 1,9 GB raw (2020-01→2026-06, auction+contract+notice)· βρέθηκαν+διορθώθηκαν 2 μη καταγεγραμμένα κενά (`auction_2025_04`, `auction_2020_08`) και επιβεβαιώθηκαν 2 **μόνιμα** κενά (`auction_2021_02`, `auction_2025_08`, server-side timeout -- βλ. νέα §4.9 «Κάλυψη δεδομένων»). Το pipeline re-run μετατόπισε ουσιωδώς τους εθνικούς αριθμούς: **8.647→12.827 ΑΦΜ φορέων**, **3.217→6.191 προφίλ** `/foreas/`· το εθνικό DA% παρέμεινε σταθερό (75-84%/έτος) πριν/μετά, καμία ένδειξη ότι το audit/repair αλλοίωσε κατανομές. Τεκμηριώθηκαν επίσης δύο αποφάσεις που είχαν μείνει ανεκτέλεστες (tech_report v2, B2/B3): **§4.2** πλέον δηλώνει ρητά την παραδοχή πίστωσης όλης της αξίας συμβάσεων-κοινοπραξιών στον πρώτο καταχωρημένο ανάδοχο και ο δείκτης `pct_multi_member` δημοσιεύεται δίπλα στο HHI/top1· **§3.2** διακρίνει πλέον ρητά μορφή (`pct_org_vat`) από mod-11 checksum (`pct_org_vat_checksum`, νέα `is_valid_vat_checksum()` στο `kimdis_data.py`) αντί να τα συγχέει σε μία υπόσχεση που δεν τηρούνταν στον κώδικα. |
| 2026-07-10 | 0.5 | **D2: πρώτη υλοποίηση §4.7 (σύντομες προθεσμίες υποβολής)** σε `scripts/compute_indicators_v1.py::deadline_indicator()` → `data/processed/indicator_deadlines.csv` (10.435 γραμμές φορέα/έτους, `vat, year, n_notices, median_deadline_days, pct_short_deadline, coverage_pct, note`). Οριστικοποιήθηκε: `days = finalSubmissionDate − submissionDate` (ημερολογιακή ανάλυση), αποκλεισμός απευθείας αναθέσεων (84% των notice, μη-ανταγωνιστική ημερομηνιακή σημασιολογία), ελάχιστο N=5, guard σε NaT/αρνητικές διάρκειες με δημοσιευμένο coverage_pct. **Συνειδητή απόφαση χρήστη:** το `pct_short_deadline` (% σύντομων προθεσμιών) **δεν υλοποιείται σε αυτό το πέρασμα** -- απαιτεί πίνακα ελάχιστων νόμιμων προθεσμιών ανά τύπο διαδικασίας που δεν υπάρχει τεκμηριωμένος και χρειάζεται νομική επιβεβαίωση, ίδιο άνοιχτο ζήτημα με τα όρια bid-splitting (§4.5). Η στήλη δημοσιεύεται με τιμή `null` προς το παρόν. Ενσωματώθηκε στο downstream pipeline (`build_site_data.py`, `build_foreas_data.py`) και νέα κάρτα «Διάμεση προθεσμία υποβολής» στο `/foreas/<vat>/` (build-time inline SVG, ουδέτερη μπλε κλίμακα, badge «Ανεπαρκή δεδομένα» κάτω από N=5). |
